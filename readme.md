# An Overenginnered To-Do App
**a simple todo app i made to learn fastapi**
## How to run
run the batch file and it should automaticly run the app.
if you want to do it manually however, run the venv in the backend folder, run `uvicorn main:app --reload`, then run the html file in the frontend